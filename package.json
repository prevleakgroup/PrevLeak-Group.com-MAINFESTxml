{
  "name": "prevleak-group",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces"
  },
  "engines": {
    "node": ">=18"
  }
}
