{
  "name": "prevleak-group",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "lucide": "^1.28.0"
  }
}
